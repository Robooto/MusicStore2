@model MusicStore2.Models.Genre

@{
    ViewBag.Title = "Browse";
}

<h2>Browse Genre: @Model.Name</h2>

<ul>
    @foreach (var album in Model.Albums)
    {
        <li>
            @album.Title
        </li>
    }
</ul>
